public void siteEvent(Hline h)
{
  
}
	//When a site event occurs, updates the beach line, adding the vertex to the arrays of sites
	
//Anupa
	public void circleEvent(Hline h)
	//updates the beach line, adding vertices to sites

